providers = ["node"]


[phases.setup]
nixPkgs = ["nodejs_20", "pnpm", "openssl"]

[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]


[phases.build]
cmds = [
  "npx prisma generate",
  "pnpm run build"
]

[start]
cmd = "npx prisma generate && pnpm start"
